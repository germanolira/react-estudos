type Icon = '🌮' | '🍟' | '🍦' | '🥑';
// type Icon = 'gray' | 'white' | 'blue' | 'red';
type Size = 'xl' | 'lg' | 'md' | 'sm';

type SizedIcon = `${Size}-${Icon}`;

const magic:SizedIcon = ''

// type Color = 'red' | 'green' | 'blue' | 'yellow';
// type NumberVariation = '.100' | '.200' | '.300' | '.400';

// type ColorNumber = `${Color}${NumberVariation}`;

// const magic:ColorNumber = ''